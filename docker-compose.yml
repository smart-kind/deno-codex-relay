services:
  deno-deepseek:
    build: .
    container_name: codex-relay-deno
    ports:
      - "17150:7150"
    volumes:
      - ./relay-config.json:/app/relay-config.json:ro
    environment:
      - RUST_LOG=info