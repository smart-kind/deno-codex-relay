services:
  codex-relay:
    build: .
    container_name: codex-relay
    ports:
      - "17150:7150"
    volumes:
      - ./relay-config.json:/app/relay-config.json:ro
      - ./data:/app/data
    environment:
      - LOG_LEVEL=info
    restart: unless-stopped